{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug: Chrome against dev server",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "dev"
    }
  ]
}
